"use client";
import Link from "next/link";
import { useState } from "react";
import { RiskRegisterRecord } from "@/lib/grc/risk-register";
import { useRiskRegister } from "@/components/risk-register-provider";

const badge: Record<RiskRegisterRecord["riskRating"], string> = { Low: "bg-emerald-50 text-emerald-900", Medium: "bg-amber-50 text-amber-900", High: "bg-orange-50 text-orange-900", Critical: "bg-red-50 text-red-900" };
const labels: Record<keyof RiskRegisterRecord, string> = { id: "Risk ID", asset: "Asset", businessOwner: "Business Owner", riskStatement: "Risk Statement", threat: "Threat / Risk Event", vulnerability: "Vulnerability or Contributing Condition", businessImpact: "Business Impact / Consequence", existingControls: "Existing Controls", likelihood: "Final Likelihood", impact: "Final Impact", riskScore: "Risk Score", riskRating: "Risk Rating", treatmentStrategy: "Treatment Strategy", treatmentPlan: "Treatment Action / Plan", riskOwner: "Risk Owner", targetDate: "Target Date", status: "Status", rationale: "Analyst Rationale" };
export default function RiskRegisterPage() { const { records } = useRiskRegister(); const [selected, setSelected] = useState<RiskRegisterRecord | null>(null); return <main className="min-h-screen bg-slate-50 px-5 py-8 text-slate-900 sm:px-10 lg:px-16"><div className="mx-auto max-w-7xl"><nav className="flex items-center justify-between"><Link className="text-lg font-semibold" href="/">AI-GRC Workflows</Link><Link className="text-sm font-medium text-slate-600" href="/risk-assessment">New assessment</Link></nav><header className="mt-12"><p className="text-sm font-semibold uppercase tracking-[.16em] text-teal-700">Risk Register</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">Human-approved risk records</h1><p className="mt-3 text-slate-600">Records exist only for this browser session and are not persisted.</p></header>{records.length === 0 ? <section className="mt-8 rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center"><h2 className="text-xl font-semibold">No risk records yet</h2><p className="mt-2 text-slate-600">Complete an analyst review, then deliberately add it to the register.</p><Link className="mt-5 inline-block rounded-lg bg-slate-900 px-4 py-2.5 text-sm font-semibold text-white" href="/risk-assessment">Open assessment</Link></section> : <><section className="mt-8 overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm"><table className="w-full min-w-240 text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{["Risk ID", "Asset", "Risk statement", "Owner", "L", "I", "Score", "Rating", "Treatment", "Status"].map((head) => <th className="px-4 py-3 font-semibold" key={head}>{head}</th>)}</tr></thead><tbody>{records.map((record) => <tr className="border-t border-slate-100" key={record.id}><td className="px-4 py-4 font-medium">{record.id}</td><td className="px-4 py-4">{record.asset}</td><td className="max-w-80 px-4 py-4">{record.riskStatement}</td><td className="px-4 py-4">{record.riskOwner || "—"}</td><td className="px-4 py-4">{record.likelihood}</td><td className="px-4 py-4">{record.impact}</td><td className="px-4 py-4 font-semibold">{record.riskScore}</td><td className="px-4 py-4"><span className={`rounded-full px-2 py-1 text-xs font-semibold ${badge[record.riskRating]}`}>{record.riskRating}</span></td><td className="px-4 py-4">{record.treatmentStrategy}</td><td className="px-4 py-4">{record.status}</td><td className="px-4 py-4"><button className="font-semibold text-teal-800 underline" onClick={() => setSelected(record)}>View</button></td></tr>)}</tbody></table></section>{selected && <section className="mt-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-start justify-between"><div><p className="text-sm font-semibold text-teal-700">{selected.id}</p><h2 className="mt-1 text-2xl font-semibold">Complete risk record</h2></div><button className="text-sm font-medium underline" onClick={() => setSelected(null)}>Close</button></div><div className="mt-6 grid gap-5 sm:grid-cols-2">{(Object.entries(selected) as [keyof RiskRegisterRecord, string | number][]).map(([key, value]) => <div key={key}><p className="text-xs font-medium uppercase tracking-wide text-slate-500">{labels[key]}</p><p className="mt-1 whitespace-pre-wrap text-sm">{String(value) || "—"}</p></div>)}</div></section>}</>}</div></main>; }
