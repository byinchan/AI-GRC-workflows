"use client";
import { createContext, ReactNode, useContext, useState } from "react";
import { appendRiskRecord, RiskRegisterRecord } from "@/lib/grc/risk-register";
import { createSubmission, markSubmissionRegistered, ReviewSubmission } from "@/lib/grc/review-queue";
const RegisterContext = createContext<{ records: RiskRegisterRecord[]; addRecord: (record: RiskRegisterRecord) => void; submissions: ReviewSubmission[]; addSubmission: (source: Record<string,string>) => ReviewSubmission; activeSubmission: ReviewSubmission | null; openSubmission: (id: string) => void; markRegistered: (id: string, riskId: string) => void; clearActiveSubmission: () => void } | null>(null);
export function RiskRegisterProvider({ children }: { children: ReactNode }) { const [records, setRecords] = useState<RiskRegisterRecord[]>([]); const [submissions, setSubmissions] = useState<ReviewSubmission[]>([]); const [activeSubmission, setActiveSubmission] = useState<ReviewSubmission | null>(null); const addRecord = (record: RiskRegisterRecord) => setRecords((current) => appendRiskRecord(current, record)); const addSubmission = (source: Record<string,string>) => { const submission = createSubmission(source, submissions); setSubmissions((current) => [...current, submission]); return submission; }; const openSubmission = (id:string) => setActiveSubmission(submissions.find((submission) => submission.id === id) ?? null); const markRegistered = (id:string, riskId:string) => { setSubmissions((current) => markSubmissionRegistered(current, id, riskId)); setActiveSubmission(null); }; return <RegisterContext.Provider value={{ records, addRecord, submissions, addSubmission, activeSubmission, openSubmission, markRegistered, clearActiveSubmission: () => setActiveSubmission(null) }}>{children}</RegisterContext.Provider>; }
export function useRiskRegister() { const context = useContext(RegisterContext); if (!context) throw new Error("RiskRegisterProvider is required"); return context; }
